# ----------------------------------------------------------
# 🌍 Universal Git Attributes Configuration
# Ensures consistent behavior across all environments
# ----------------------------------------------------------

# Normalize line endings to LF in repo, auto-convert on checkout
* text=auto eol=lf

# Treat all JS, TS, JSON, HTML, and SCSS files as text
*.js     text
*.jsx    text
*.ts     text
*.tsx    text
*.json   text
*.html   text
*.css    text
*.scss   text

# Mark binary files to prevent corruption
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.svg    binary
*.ico    binary
*.pdf    binary
*.zip    binary
*.tar    binary
*.gz     binary

# Prevent diffing or text conversion for lock and build files
package-lock.json -diff
yarn.lock          -diff
pnpm-lock.yaml     -diff
dist/*             -diff

# Enforce UTF-8 for text files
*.md text eol=lf working-tree-encoding=UTF-8

# Merge conflict safety for key config files
webpack.config.js  merge=union
tsconfig.json      merge=union
package.json       merge=union
